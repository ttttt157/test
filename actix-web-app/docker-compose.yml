services:
  # サービス定義(起動後に指定で使うことある)
  actix-web-app:
    # Dockerfile指定してビルド(contextでディレクトリ指定)
    build:
      context: .
    # コンテナ名(起動後に指定で使うことある)
    container_name: actix-web-app
    # ホスト : コンテナ でポート紐づけ
    ports:
      - "8280:8280"
    # 使用する環境変数ファイル
    env_file:
      - ../.env
    # コンテナ停止時に自動再起動(手動停止は除く)
    restart: unless-stopped